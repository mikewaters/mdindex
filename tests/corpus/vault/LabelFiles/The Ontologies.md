# The Ontologies

Is a:: Data Store?

## Information Standards

- [ ] decide whether to use `ref:`, `#ref`, or `ref::` across my systems

Use the tag `#ref` to denote a reference hyperlink; using a tag allows for two-way linking as well as aggregation.

Use a double-colon `::` suffix on attribute names

- this will present as an Attribute in Roam, but denote a tag in Heptabase.

## Information Architecture

### Attributes (global)

Is a::

Contains::

Categories::

Alias::

### Entity-specific Attributes

#### Components

Implementation::

#### Workflow Components

Data in::

Data out::






