"""REMOVED: This module has been deleted.

Import from pmd.metadata instead:
    from pmd.metadata import DocumentMetadataRepository, StoredDocumentMetadata
"""

raise ImportError(
    "pmd.store.document_metadata has been removed. "
    "Use 'from pmd.metadata import DocumentMetadataRepository, StoredDocumentMetadata' instead."
)
